# 전국 어린이보호구역 지도 앱 — PRD (Product Requirements Document)

- 문서버전: v1.0
- 작성일: 2026-08-05
- 작성자: Claude
- 데이터 출처: 전국어린이보호구역표준데이터.csv (공공데이터, 데이터기준일자 2026-07-15)

---

## 1. 개요 (Overview)

전국 14,647개 어린이보호구역(초등학교, 유치원, 어린이집 등) 데이터를 지도 위에 시각화하여,
CCTV 설치 여부를 한눈에 확인하고, 시설 유형별로 필터링하며, 두 지점 간 이동 시
어린이보호구역을 최대한 경유하는 "안전 경로"를 안내받을 수 있는 웹 애플리케이션.

### 1.1 목적
- 학부모, 지자체, 경찰 등이 어린이보호구역의 CCTV 설치 현황을 지도 기반으로 파악
- 특정 시설종류/CCTV 설치여부 조건으로 필터링하여 사각지대(미설치 구역) 식별
- 목적지 이동 시 어린이보호구역을 경유하도록 유도하는 경로 안내 제공(운전자 경각심 고취 목적)

### 1.2 핵심 기술 스택 (중요 — 필수 준수)
> **본 프로젝트는 프레임워크(React, Vue 등) 없이 순수(바닐라) HTML / CSS / JavaScript로만 구현한다.**
> - 번들러(Webpack, Vite), 트랜스파일러(Babel), npm 패키지 기반 프레임워크 사용 금지
> - 지도 라이브러리(Leaflet.js 등)는 CDN `<script>` 태그로 로드하여 사용 가능 (프레임워크가 아닌 순수 JS 라이브러리이므로 허용)
> - 파일 구성: `index.html`, `style.css`, `app.js`(또는 기능별 분리: `map.js`, `filter.js`, `route.js`) 형태의 정적 파일 구조
> - 별도 백엔드 서버 없이 정적 파일(csv → json 변환본 포함)만으로 브라우저에서 동작

---

## 2. 데이터 명세 (Data Specification)

### 2.1 원본 파일
- 파일명: `전국어린이보호구역표준데이터.csv`
- 인코딩: CP949 (EUC-KR 계열) → 웹 사용을 위해 **UTF-8 JSON으로 사전 변환 필요**
- 총 행 수: 14,647건
- 총 컬럼 수: 14개

### 2.2 컬럼 정의 및 활용 방식

| 컬럼명 | 설명 | 예시 | 앱 내 활용 |
|---|---|---|---|
| 시설종류 | 보호구역 시설 유형 | 초등학교, 유치원, 어린이집, 특수학교, 학원 등 | 필터 옵션 |
| 대상시설명 | 시설 이름 | 리틀킹어린이집 | 팝업 표시 |
| 소재지도로명주소 | 도로명 주소 | 경기도 이천시 증신로269번길 31 | 팝업 표시(주소) |
| 소재지지번주소 | 지번 주소 | 경기도 이천시 송정동 106-38 | 도로명 주소 없을 시 대체 표시 |
| 위도 | 위도 좌표 | 37.293720 | 마커 좌표 |
| 경도 | 경도 좌표 | 127.440823 | 마커 좌표 |
| 관리기관명 | 관리 지자체 | 경기도 이천시청 | (보조정보) |
| 관할경찰서명 | 관할 경찰서 | 이천경찰서 | 팝업 표시 |
| CCTV설치여부 | Y/N | Y | 마커 색상 결정 (필터 옵션) |
| CCTV설치대수 | 설치 대수 | 1.0 | (보조정보, 팝업 추가 표시 가능) |
| 보호구역도로폭 | 도로 폭(m), 일부 "9~20"처럼 범위 표기 | 8, 9~20 | 팝업 표시 |
| 데이터기준일자 | 갱신일 | 2026-07-15 | (보조정보) |
| 제공기관코드/명 | 제공 지자체 | 경기도 이천시 | (보조정보) |

### 2.3 데이터 특이사항 (구현 시 유의)
- `시설종류` 값 중 `초등학교+어린이집`처럼 복합값 존재 → 필터링 시 별도 카테고리로 처리하거나 다중 매칭 처리
- `보호구역도로폭`이 `9~20`처럼 범위 문자열인 경우 존재 → 숫자 정렬/계산에 사용하지 않고 **문자열 그대로 표시**
- 위도/경도 결측치는 없음(0건) → 별도 결측 필터링 로직은 필요 없으나, 방어 코드는 권장
- 위도 범위: 약 33.16 ~ 38.49 / 경도 범위: 약 124.66 ~ 130.90 (전국 + 도서지역 포함)
- 데이터 건수가 14,647건으로 많아 **전체를 지도에 동시 렌더링 시 성능 저하 가능** → 클러스터링(마커 클러스터) 적용 필수 검토

---

## 3. 기능 요구사항 (Functional Requirements)

### FR-1. 지도 및 마커 표시
- 지도 라이브러리: Leaflet.js (CDN 로드, OpenStreetMap 타일 사용)
- CSV 내 위도/경도 기준으로 전국 14,647건 마커 렌더링
- 대량 마커 성능 문제 해결을 위해 **마커 클러스터링(Leaflet.markercluster)** 적용
- 최초 진입 시 대한민국 전역이 보이는 초기 뷰(중심 좌표/줌 레벨) 설정

### FR-2. 마커 색상 구분
- `CCTV설치여부 = Y` → **파란색 마커**
- `CCTV설치여부 = N` → **빨간색 마커**
- 색상 구분은 클러스터 내부에서도 유지(클러스터 아이콘은 혼합 시 기본 클러스터 스타일 사용 가능)

### FR-3. 사이드바 필터
- **시설종류 필터**: 체크박스 또는 다중선택 드롭다운
  - 옵션: 초등학교, 유치원, 어린이집, 특수학교, 학원, 대안학교, 외국인학교, 국제학교, 초등학교+어린이집, 대안교육기관, 외국인교육기관 (전체 선택/해제 포함)
- **CCTV설치여부 필터**: 전체 / 설치(Y) / 미설치(N) 라디오 버튼 또는 토글
- 필터 변경 시 지도 마커 즉시 갱신(리렌더링)
- 필터 결과 건수를 사이드바 상단에 표시 (예: "필터 결과: 1,234건")

### FR-4. 마커 팝업(Popup)
마커 클릭 시 아래 4개 정보 표시:
1. 대상시설명
2. 주소 (소재지도로명주소, 없으면 소재지지번주소로 대체)
3. 보호구역도로폭
4. 관할경찰서명

(선택 확장: CCTV설치여부/설치대수도 함께 표시 가능)

### FR-5. 안전 경로 안내 (출발지 → 도착지, 어린이보호구역 우선 경유)
- 사용자가 출발지/도착지를 지정하는 방법:
  - 지도 클릭으로 출발지/도착지 지정, 또는
  - 주소 검색 입력(지오코딩 API 필요 — 예: OSRM/Nominatim 등 무료 API 활용)
- 경로 계산 방식(택1 또는 단계적 구현):
  - **1단계(기본)**: 외부 라우팅 API(OSRM 등, 무료/공개 서버 또는 자체 서버)로 "최단 경로"를 우선 제공
  - **2단계(심화)**: 어린이보호구역 좌표를 경유지(waypoint)로 활용하여, 출발-도착 사이에 위치한 보호구역을 경유하도록 경로를 우회 생성하는 "안전 우선 경로" 옵션 제공
- 화면에 "최단 경로"와 "보호구역 경유 경로"를 동시 비교 표시(거리/예상 시간 함께 노출)
- 경로 상에 포함된 어린이보호구역 개수를 결과에 표시(예: "이 경로는 보호구역 5곳을 경유합니다")

> ⚠️ 참고: 실제 도로 네트워크 기반 최단/우회 경로 계산은 클라이언트 단독으로는 한계가 있어
> 외부 라우팅 서비스(OSRM Demo Server 등) 연동이 필요하며, 무료 공개 서버는 요청 제한이 있을 수 있음.
> PRD 승인 후 구현 단계에서 라우팅 API 옵션을 확정한다.

---

## 4. 비기능 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 |
|---|---|
| 기술 스택 | 바닐라 HTML/CSS/JavaScript (프레임워크 미사용), Leaflet.js는 CDN으로 허용 |
| 브라우저 지원 | 최신 Chrome, Edge, Safari |
| 성능 | 14,647건 마커를 클러스터링으로 3초 이내 초기 렌더링 |
| 반응형 | 데스크톱 우선, 최소한의 모바일 대응(사이드바 접힘 등) |
| 데이터 처리 | CSV(CP949) → UTF-8 JSON 사전 변환 스크립트 포함 (빌드 시 1회 실행) |
| 오프라인 여부 | 지도 타일/라우팅 API는 온라인 필요, 나머지는 정적 파일로 동작 |

---

## 5. 화면 구성 (UI 구조)

```
┌─────────────────────────────────────────────┐
│  Header: 전국 어린이보호구역 안전지도           │
├───────────────┬─────────────────────────────┤
│  사이드바        │                              │
│  - 시설종류 필터  │                              │
│  - CCTV 필터    │         지도 (Leaflet)         │
│  - 결과 건수     │      (마커 + 클러스터)          │
│  - 경로 안내 UI  │                              │
│    (출발/도착)   │                              │
└───────────────┴─────────────────────────────┘
```

---

## 6. 파일 구조 (예정)

```
/project
 ├─ index.html
 ├─ style.css
 ├─ /js
 │   ├─ app.js          (초기화/전체 흐름 제어)
 │   ├─ map.js           (Leaflet 지도/마커/클러스터 렌더링)
 │   ├─ filter.js        (사이드바 필터 로직)
 │   ├─ popup.js         (팝업 콘텐츠 생성)
 │   └─ route.js         (경로 탐색: 최단/보호구역 우선)
 ├─ /data
 │   └─ child_safety_zones.json   (CSV → UTF-8 JSON 변환본)
 └─ PRD.md
```

---

## 7. 단계별 개발 범위 (제안)

| 단계 | 범위 |
|---|---|
| Step 1 | CSV → JSON 변환, 지도 초기화, 전체 마커(클러스터) 표시, CCTV 색상 구분 |
| Step 2 | 사이드바 필터(시설종류/CCTV) 구현 및 지도 연동 |
| Step 3 | 마커 팝업(4개 정보) 구현 |
| Step 4 | 출발지/도착지 지정 + 최단 경로 안내 |
| Step 5 | 보호구역 우선 경유 경로 로직 추가 및 두 경로 비교 UI |

---

## 8. 열린 이슈 (Open Questions)

1. 경로 안내(FR-5)에 사용할 라우팅 API를 무엇으로 확정할지 (OSRM 공개 데모 서버 사용 시 상용/대량 트래픽 제약 있음)
2. 지오코딩(주소 → 좌표 변환)이 필요한지, 아니면 지도 클릭만으로 출발/도착을 지정할지
3. "보호구역 경유"의 기준 — 경로에서 반경 몇 m 이내에 보호구역이 있으면 "경유"로 인정할지
4. 14,647건 전체를 한 번에 로드할지, 지도 뷰포트(bounds) 기준으로 필요한 데이터만 지연 로드할지

---

승인 후 Step 1부터 순차적으로 구현을 시작합니다.
